const INITIAL_COLOR = "#cdcdcd";

const recordingConfig = {
  recordCanvas: true,
  collectFonts: true,
  sampling: {
    canvas: "10",
  },
} as const;

export { INITIAL_COLOR, recordingConfig };
